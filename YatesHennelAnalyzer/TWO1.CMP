#include <stdio.h>                                                         
  main ( )                                                                  
  {                                                                        
   int initial, final, max;
  
   printf("Type the initial number ");
1   scanf("%d", &initial);      

   printf("Type the final number ");
   scanf("%d", &final);

   if ( final <= 20 ) {

-----------------------------------------------------------------------
2     
     max = initial + final ; 
-------------------------------------------------------------------------
     while  (initial <= final)                   
3      {   
--------------------------------------------------------------------------    
4        if (max == 12)  {
--------------------------------------------------------------------------
5          if (initial>max) 
---------------------------------------------------------------------------
6            max = max + initial;            
          else
----------------------------------------------------------------------------
7            max = max - initial;
------------------------------------------------------------------------------
8        } 
-----------------------------------------------
9        if (max > 15) {
-----------------------------------------------
           printf ("Type a number ");
10           scanf("%d", &max);
        }    
---------------------------------------
11        initial++;       
      }
--------------------------------------
12   }
------------------------------------

13}                                                                          
